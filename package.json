{
  "name": "defendml-worker-release-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": "25.8.1", "npm": "11.11.0" },
  "packageManager": "npm@11.11.0",
  "scripts": {
    "test": "node --test tests/*.test.mjs && node attack-authorization-grant-test.mjs",
    "provenance:build": "node scripts/provenance.mjs build",
    "provenance:record": "node scripts/provenance.mjs record",
    "worker:deploy": "node scripts/provenance.mjs deploy"
  },
  "devDependencies": { "esbuild": "0.28.1", "wrangler": "4.111.0" }
}
